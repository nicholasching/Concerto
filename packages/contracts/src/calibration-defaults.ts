// Frozen into each new run; previously recorded runs retain their own palette.
export const DEFAULT_CALIBRATION_PALETTE = {
  paletteVersion: "red-blue-v1",
  palette: { zero: "#FF0000", one: "#0066FF", neutral: "#111111" },
} as const;
