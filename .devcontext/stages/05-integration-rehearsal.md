# Stage 05 - Integration and rehearsal

Status: not started. Owner: captain and all human leads. Base: foundation-v1.

## Merge checkpoints

1. Clock + real join/unlock/click (Teams 1/2): two phones and source characterization.
2. Packet + clean real clip (Teams 2/3): actual accepted IDs and explicit rejects.
3. Fixture map + selection + assignments + four-channel sound (Teams 1/2/4).
4. Upload/worker/manual geometry/review/map commit (Teams 1/3/4): decoded ID controls that phone.
5. Transport/mix/recovery, load with uploads/jobs, real mobile/camera/network/acoustic tests.
6. Three rehearsals, fallback drills and a frozen demonstration tag.

Merge producers before consumers; all gates for shared changes. Create test:e2e and test:load only when they perform the named work; these scripts do not exist in the foundation. Mock-only success means ready for integration.

## Evidence

Apply masterplan targets/runbook. Record commit, commands/procedure, devices/OS/cameras/codecs/network/machine, hashes/seed, counts, metrics and limitations. Keep unit, synthetic, socket, browser and physical evidence distinct.

Resolve deadline/joining window, venue/layout/camera positions, phone sample and prepared stems early. Retain manual columns, a smaller participant area and the rehearsed musical fallback when measurements require them.
