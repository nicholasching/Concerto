# Admin console implementation journal

Date: 2026-09-19. Agent: Claude. Branch: feat/admin-console. Baseline: foundation-v1 (ab59c27).
Bun: 1.4.2 (masterplan pins 1.3.14; 1.4.2 runs the gate clean on this machine).

## Goal

Implement the full admin console per `plan.md` and `.devcontext/teams/admin-console/subplot.md`:
selection math, typed adapter, calibration, assignment, timeline, and a full mock walkthrough.
Build against the fake-input harness in `tools/admin-demo/`. Own only `admin-frontend/`,
`packages/selection/`, `tools/admin-demo/`.

## Session start state

- Rebuilt `feat/admin-console` from `main` (ab59c27). No feature code yet.
- `plan.md` and `subplot.md` written and reviewed with the user.
- Baseline `bun run gate:admin` passes after clearing a stale `admin-frontend/.next` cache left by
  the earlier deleted work. Note: always `rm -rf admin-frontend/.next` if the build references a
  page that no longer exists.
- bun is at `~/.bun/bin/bun.exe`, not on the bash PATH; prefix commands with
  `export PATH="$HOME/.bun/bin:$PATH"`.

## Plan (slices)

1. Selection math (pure) -> verify: gate:admin + new selection tests.
2. Typed adapter + harness routes -> verify: adapter tests pass.
3. Calibration UI + harness -> verify: walkthrough, one failed upload keeps others.
4. Assignment UI + harness -> verify: select, assign, schedule, undo.
5. Timeline UI + harness -> verify: shared playhead, panic.
6. Full walkthrough test -> verify: gate runs walkthrough, performance recorded.

## Log

- Started Slice 1.
- Slice 1 (selection math): pure rectangle + polygon, inclusive borders, orientation, coarse/unseen
  excluded, 1,500-point < 100ms. 8 tests pass. Committed.
- Slice 2 (typed adapter + harness): adapter tracks pending/confirmed, surfaces errors, rejects
  stale map. Harness built on plain Bun.serve (hono not hoisted to root; root package.json is
  captain-owned so I avoided adding hono there). Inlined epochNow in the harness to avoid a
  @orchestra/sync dep from tools. 6 adapter tests pass. Committed.
- Slices 3-5 (UI): Calibration, Review, Assign, Perform panels + Session header. Canvas map for
  1,500-dot performance. Shared-clock playhead via snapshot.serverMs (documented as not a second
  estimator). admin-frontend now depends on @orchestra/selection — lockfile changed, noted for
  captain review. Gate passes; build succeeds.
- Slice 6 (walkthrough): automated end-to-end test against the harness — uploads -> commit ->
  assignment -> play -> stop -> panic, plus stale-map rejection. 8 admin tests total pass.
- Verified live: started `bun run dev:admin-demo`, confirmed console renders at :3001, harness
  serves 1,500-phone snapshot, STALE_MAP rejection works over HTTP. Stopped background servers.
- Note: stale `admin-frontend/.next` cache from the earlier deleted work breaks the Next build
  (references a removed calibration page). Always `rm -rf admin-frontend/.next` if the build
  reports a missing page module.
- Note: bun is at `~/.bun/bin/bun.exe`, not on the bash PATH. Prefix commands with
  `export PATH="$HOME/.bun/bin:$PATH"`. bun 1.4.2 here (masterplan pins 1.3.14).

## Result

All six slices implemented and mock-verified. `bun run gate:admin` passes. The console is fully
clickable against the fake-input harness with no real files. Status set to "ready for
integration"; physical/producer evidence remains outstanding by design.
