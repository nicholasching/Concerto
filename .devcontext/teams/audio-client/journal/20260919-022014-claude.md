# audio-client journal: slice 1 planning
Status: in progress
Owner / branch / baseline SHA: Claude agent for Team 2 / feat/audio-client / foundation-v1 = ab59c27105627977ee52dc2bcd4276b4532b9e2a

## Session start (2026-09-19 02:20)

- Goal: stage 02 slice 1. One AudioContext, tap unlock, hash-verified preload, scheduled click against an injected clock.
- Workflow: masterplan → subplan ([plans/01](../plans/01-audio-context-preload-click.md)) → user review → implement.
- Working tree clean at session start.
- Owned files: `packages/audio/`, `client-frontend/`, `tools/client-demo/`, Team 2 `.devcontext` files.
- Assumptions:
  - Team 1's estimator isn't available. Tests use testkit `FakeClock`. The browser demo uses a labeled local clock (`epochNow`, offset 0).
  - Desktop browser checks don't count as phone evidence.
- Verification command: `bun run gate:client`.

## Log

- 02:20 Read AGENTS.md, rules.md, masterplan, .devcontext index, stage 02, Team 2 status/handoff, BeatSync `audioContextManager.ts` and `global.tsx` load/schedule paths. Subplan written. Waiting on user review before coding.
- 02:21 User review decisions: drop legacy iOS silent-audio hack; 64 MB decoded budget; include client demo and workspace dep. Subplan marked approved.
- Bun wasn't on PATH. Installed Bun 1.3.14 into the session scratchpad (not global). `bun install --frozen-lockfile`, then baseline `bun run gate:client`: PASS.
- Implemented `packages/audio/src/{context,timing,assets,schedule}.ts` and tests. `bun test packages/audio`: 17 pass.
- Added `@orchestra/audio` and `@orchestra/sync` to client-frontend. `bun install` changed 2 workspace lines in `bun.lock` (captain review).
- `bun run gate:client`: PASS (18 tests, build ok).
- Browser check: `bun run dev:client-demo` failed because port 3000 was already in use by an unrelated local process, which I left alone. Ran the mock on 18081 and `next dev` on 3100 with the same env vars. Coordinate clicks missed the small buttons; JS `click()` on the real buttons worked. Result: "tone-0 decoded, sha256 verified (1.54 MB decoded)". Decoded at the 48 kHz context rate, so 8 s × 48000 × 4 B. Play: "Tone scheduled at audio time 11.190 s (3 s ahead)". Second press cancelled and rescheduled. Audibility not confirmed by the agent.
- Updated provenance, THIRD_PARTY_NOTICES sentence, status, handoff, and the shell sentence that said the page plays no sound.

## End of session

- State: slice 1 ready for integration. Unverified: audible output, phones, two-phone sync, acoustic onset.
- Next: user listens to the desktop tone. Phone check over a reachable origin. Subplan 02 (join/resume).

## Slice 2 planning

- Slice 1 pushed as c03cb5b. User confirmed they heard the tone in desktop Chrome. Status/journal not yet updated with that.
- Read testkit mock server, contracts (join, snapshot, device.status), protocol notes, BeatSync `useWebSocketReconnection.ts` and `websocket/dispatch.ts`.
- Wrote [subplan 02](../plans/02-join-resume-connection.md). Waiting on user review before coding.
- Subplan 02 review decisions: `?token=` socket auth as a Team 1 proposal; bad token → rejoin with notice; preload in slice 2; mock test in client-frontend/tests.
- User asked that no commit or push carry Claude attribution. Set `attribution` to empty in `.claude/settings.local.json` (git-ignored) and saved it to agent memory.

## Slice 2 implementation

- Mock: `tools/client-demo/server.ts` (own Bun server; testkit's mock returns 501 for join). `tools/` can't resolve `@orchestra/sync`, so the mock computes `performance.timeOrigin + performance.now()` directly.
- Client: `lib/join.ts`, `lib/connection.ts`, `lib/readiness.ts`, page rewrite, `preloadTracks` in `packages/audio`. The browser `WebSocket` type didn't fit the socket interface, so `browserSocket()` adapts it.
- First test run: 51/52. The failing case was my expectation: after a successful join, a stalled socket reports "reconnecting", not "joining". Test fixed, code unchanged. Then 52/52 three times in a row.
- `bun run gate:client`: PASS (53 tests, build ok).
- Browser: a second `next dev` in the same folder is refused, and the user's old `dev:client-demo` (slice 1 mock) was running. With the user's OK I stopped PIDs 28097/28099 and restarted `bun run dev:client-demo`, which is left running. Walkthrough results are in the handoff. Real clicks on the small buttons missed again; JS `click()` on the real buttons worked. The automation window stayed `visibilityState: hidden`, so the foreground switch wasn't observed.

## End of slice 2

- State: slices 1 and 2 ready for integration. Unverified: real backend/clock, phones, foreground switch.
- Next: subplan 03 (calibration flash renderer); Team 1 to review the handoff proposals.

## Slice 3 planning

- Slice 2 pushed as 45625bc (no attribution lines).
- Read `otc.ts`, OTC/playback schema notes, calibration fixture messages. Found no client message for a manual column choice (contract gap, captain-owned).
- Wrote [subplan 03](../plans/03-calibration-flash.md). User decisions: fixture clock in mock only; Skip now plus an ADR proposal for `participant.column`; countdown until 1 s before start.

## Slice 3 implementation

- Added `lib/calibration.ts` (slot maths, palette mapping, `CalibrationSession`), `lib/flash-renderer.ts`, `calibration-overlay.tsx`, an `onMessage` hook on the connection, page wiring, and mock `/__mock__/calibrate` + `/__mock__/calibration`.
- Found during implementation: a phone that skips after saying ready would still be counted by the server. The session now withdraws with `ready: false, reason: "opted-out"`.
- First run 70/71. My skipped-frames test also jumped slot 11 → 40, a larger skip than the one I asserted; the renderer correctly reported the worst lateness (5610 ms). Fixed the expectation. Then 71/71 three times. Typecheck then flagged two test-only type errors; fixed.
- `bun run gate:client`: PASS (71 tests, build ok).
- Browser: restarted my own `dev:client-demo` (the slice 2 mock had no calibrate route). Chrome automation window: `visibilityState: hidden`, 0 animation frames per second. `/__mock__/calibrate` → phone answered not ready, reason `hidden`, no arm, no flash. Correct, but the visible flash is unobserved.
- Wrote proposed ADR `20260919-113215-audio-client-manual-column.md`.

## End of slice 3

- State: slices 1-3 ready for integration. Pending: a person watches the flash once; phones; clips for Team 3; ADR agreement.
- Next: subplan 04 (channel playback).

## Test pass on request (after slice 3 commit 1a73099)

- `bun run gate:client`: PASS (71 tests).
- User's visible tab, `mock-run-2`: device 0 ready, `completed: true`, max frame lateness 8.6 ms. Device 1 (background tab) not ready, `hidden`. The user has not yet described what they saw.
- `mock-run-3`: both tabs hidden (user in terminal), both not ready, `hidden`. Correct.
- Automation tab (hidden, 0 animation frames; took over device 0 through shared storage): reload kept Device 0; `/__mock__/drop` → "Reconnecting (attempt 1)" → Connected, same device; Skip → next run `notReady` reason `opted-out`, label "Calibration: skipped"; "Take part" restored "waiting for the operator".
- "Enable sound" could not be tested here: `navigator.userActivation.hasBeenActive` is false for automated clicks, and `AudioContext.resume()` stayed pending (checked with a bare AudioContext, 2 s).
- Finding: while `resume()` hangs, the page shows no feedback and no error. Candidate fix: show "Unlocking audio..." and a timeout message. Not changed yet; reported to the user.
- User confirmed the flash works in their visible tab.
- Added `lib/audio-unlock.ts` (`unlockWithin`, 4 s) and page feedback: "Unlocking audio..." then "Sound didn't start. Tap the sound button again." The first wording said "Tap Enable sound again", but a suspended context relabels the button "Tap to resume sound", so I changed it. Checked in the automation window. 3 new tests; gate PASS (74 tests).
