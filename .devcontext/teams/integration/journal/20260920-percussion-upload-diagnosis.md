# Percussion audio upload HTTP 500 diagnosis
Date: 2026-09-20. User asked why percussion.mp3 failed to upload. Read-only diagnosis of live logs and upload path on main; no configuration changes or restart.
The live stderr log records two /control/api/assets attempts for percussion.mp3 with byteSize=11021760. Both hit Next's explicit request-body-exceeded-10MB warning; proxy errors report ECONNRESET/socket hang up, and Bun also reports its 10-second request timeout. The audience Next config rewrites /control to backend:8080 and has no body-size override. The admin adapter sends the complete File as one request. This explains the generic HTTP 500 shown by the UI; the file exceeds the frontend proxy cap (about 10.51 MiB), despite the backend's larger request limit. The browser decoded the MP3 to extract duration/sample rate/channels before attempting the upload.
Next step if fixing: align the proxy upload limit with the supported audio-file ceiling or implement bounded chunk uploads, test an upload exceeding 10 MiB through the same proxy, and address timeout/error reporting as needed. No upload retried, saved show changed, or physical check performed in this diagnostic task.

## Fix plan and reproduction
User explicitly requested the fix. Integration scope: both frontend proxy configurations, a focused regression test in the existing client gate, and this journal; no shared protocol changes. The installed Next 16.2.7 source confirms experimental.proxyClientMaxBodySize is the current setting and defaults to 10,485,760 bytes. Both the editor and backend asset metadata already cap encoded audio at 128 MiB.
Added a regression test using Next's actual request-body cloning and each app's real config, with exactly 11,021,760 synthetic bytes and hash comparison. Before the fix both cases fail: expected 11,021,760 bytes, received 10,485,760. Plan: set both proxy limits to 128 MiB, run focused regression and client/admin gates, then verify a disposable asset through the live HTTPS proxy without editing the saved show. Next automatically reloads config; preserve the backend epoch and live state.

## Fix and completed verification
- Both client-frontend/next.config.ts and admin-frontend/next.config.ts now set experimental.proxyClientMaxBodySize to 128 * 1024 * 1024. Next detected each config change and restarted its frontend process automatically; the backend was not restarted.
- Regression: bun test client-frontend/tests/upload-proxy.test.ts now passes 2 tests / 4 assertions. Before the fix both tests demonstrated truncation at exactly 10 MiB; afterwards both preserve the complete payload and SHA-256.
- Root gate:client and gate:admin both passed (contract/fixture/boundary checks, TypeScript, lint, contract tests, focused tests and production builds). Logs are in runtime/branch-restart-20260920/upload-gate-client.log and upload-gate-admin.log. No unrelated OTC gate rerun.
- Live HTTP verification: runtime/branch-restart-20260920/check-upload.ts uploaded an independently generated valid WAV of exactly 11,021,760 bytes through localhost:3000, localhost:3001 and https://htn.nicholasching.ca. Each returned success, full byte count and matching SHA-256; downloaded backend bytes also matched. The script removed only its disposable assets after checking they were absent from the saved show.
- Verified saved show and assignments unchanged, and backend server epoch unchanged. No real stem was added or show saved on the user's behalf. Physical playback was not tested. The public check covers this 10.51-MiB file size; separate upstream provider limits may still apply to larger files.
- Next user action: retry percussion.mp3 in the existing editor and save the show when ready. Changes are applied locally; no commit/push was requested in this fix turn.

## Publication handoff

The later automatic-section rollout verified that the real percussion.mp3 is saved in the live show and its public download matches the recorded SHA-256. The user confirmed the live changes work and explicitly requested commit/push. Publish this verified proxy fix and regression separately from the automatic-section feature; no additional runtime change is needed.
