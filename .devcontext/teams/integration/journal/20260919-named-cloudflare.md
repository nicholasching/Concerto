# Named audience tunnel

Captain follow-up on main a052e68, 2026-09-19. User authorizes configuring the client reverse proxy for `htn.nicholasching.ca` with their named-tunnel connector token. Preserve the live backend/concert and expose the existing audience origin on port 3000. Operator/camera routes remain local. Prior Quick Tunnel setup is already implemented.

Token is stored outside the repository and OneDrive in a current-user-only local application-data directory; never include its contents in source, logs, command-line arguments, context or commits. Installed cloudflared 2025.8.1 supports `--token-file`; use a hidden foreground connector process for this test, without installing a Windows service. Public hostname routing is managed by the tunnel's Cloudflare configuration.

Plan: inspect connector configuration/DNS, allow the hostname for Next development assets, update the console QR, verify public HTTPS joins/snapshots/audio and WSS clock exchange with `check:tunnel`, and inspect a real browser. Add a repeatable non-secret startup command and document the remaining physical checks.

Connector verification: the supplied token starts the intended named tunnel, which registers four QUIC connections. Cloudflare's existing remote configuration already publishes `htn.nicholasching.ca -> http://localhost:3000`, with a 404 catch-all and private-network routing disabled. DNS resolves; public `/api/health` and connector-local `/ready` return 200. No DNS/dashboard change was needed. Logs and token are in the protected local application-data directory.

Repository changes: allow the exact named host in client Next dev origins; add `bun run tunnel:named` using cloudflared's `TUNNEL_TOKEN_FILE` environment setting; document restart/setup with a non-secret `.env.example` path placeholder. Ignored root `.env` holds the token-file path and hostname/QR defaults. Next automatically reloaded its configuration; the authoritative backend stayed running with the same epoch/run/show.

The first transport check overlapped Next's config restart and returned a temporary origin connection refusal. After both local/public session endpoints returned 200, `bun run check:tunnel -- https://htn.nicholasching.ca` passed join/snapshot, blocked operator mutation/socket routes, WSS snapshot/clock exchange, and original audio byte count/SHA-256. It allocated/disconnected test ID 12 without changing routing or show. Saved the named HTTPS link through the existing console UI; the QR/link includes the current session.

Final verification: `bun run gate:client` passes 116 focused tests, 14 contract tests, shared checks and production build. A real browser at the named HTTPS URL joins as device 13, reports Connected / Clock synced (~10 ms displayed uncertainty), unlocks via Enable sound, and verifies all four audio assets. It has no channel and the transport is stopped during verification. Closed the temporary browser participant after verification; the named connector stays running. The user subsequently started calibration run 5 in the operator console; left their current view and run in place. The named QR setting remains saved. No physical phone speaker/onset or Railway evidence is claimed. `git diff --check` passes; the private token is not in tracked files.

Handoff: phones open `https://htn.nicholasching.ca/?session=dev-session`, tap Enable sound and stay visible. Keep this computer/app/connector running. After reboot, start `bun run dev:all` and `bun run tunnel:named` in separate terminals; ignored `.env` supplies this machine's private token-file path. See `docs/cloudflare-tunnel.md`. Future clones require their own private token-file setup; credentials are never distributed via Git.
