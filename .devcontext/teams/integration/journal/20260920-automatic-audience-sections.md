# Automatic four-section routing implementation
Date: 2026-09-20. Baseline main at 1e66b42. Preserve uncommitted, verified upload-limit fix and its journal/test. User requests four automatically balanced audience groups and presets in show setup, removing manual step 02.

Integration owner for this change: shared section contract/helper, backend routing/persistence, admin setup/overview and participant fallback. No worker detector/reference-tree edits. Decision: [automatic audience quartiles](../../../decisions/20260920-automatic-audience-quartiles.md).
Plan: define additive contract and deterministic balanced sections; regression-test boundary counts/ties/unknowns, map commit, presets, persistence and stale messages; connect UI and regenerate schemas/fixtures; run affected gates and browser verification in isolated data; activate safely with persistent show/IDs preserved. Prior OTC full suite stalled; do not represent it as passing or change detector tests to hide failures.

## Implementation checkpoint
Added optional show section-channel presets, persisted map memberships, per-phone section snapshot and authenticated four-section choice. Shared helper balances counts by x/y/ID, excludes unknown/coarse positions, and reports data-derived boundaries. Server commit and save-show apply routing while stopped; pending/playing commits reject. New manual choices retain null coordinates and the existing future routing/readiness path. Legacy participant.column remains compatible. Restart migration derives sections for old maps once; subsequent restarts preserve saved assignments, including cancelled pending routes.

Replaced manual Assign controls with four group cards and a read-only section-colored map. Show editor exposes four lane selectors (including silence); phone fallback offers the same four labels. Calibration keeps review and its commit button now explicitly applies sections. Root TypeScript passes. New contract tests cover uneven/small/empty/1500-phone crowds, ties, unknowns and defaults. New backend tests cover presets, automatic commits, partial recalibration, fallback, persistence and stale/injected messages.

Initial regression adjustments: an existing stale-map test attempted commit while a prior assignment was pending; it now finishes that scheduled action before committing and verifies the commit status. A persistence test compared membership array order instead of members; compare by device ID, keeping all fields. The first test command accidentally matched an old ignored isolation checkout; subsequent targeted commands use ./ paths exclusively. No assertions were weakened to conceal functional failures.

## Checks and browser evidence

- Root `gate:sync` passed: 237 backend/sync/testkit/load tests, 2,705 assertions, plus shared contract/fixture/boundary/type/lint checks and backend build. New API coverage verifies automatic routing on commit and rejects commit during playback/pending actions without changing the map.
- Root `gate:client` passed, including the real Next upload-cloning regression and client production build. Root `gate:admin` passed; after draft recovery and final map copy changes, repeated admin gate passed with 25 admin/selection tests, 65 assertions and production build. Logs: ignored `runtime/branch-restart-20260920/sections-gate-{sync,client,admin-final}.log`.
- Shared tests verify uneven distributions, deterministic ties, empty/small/1,500-device counts and legacy show defaults. Backend coverage includes save-preset rerouting, explicit silence, partial recalibration clearing unknown targets, authenticated fallback, stale epoch rejection and checkpoint compatibility.
- `.venv/Scripts/python.exe -m pytest workers/otc/tests/test_boundary.py -q`: 17 passed. Full OTC gate was not rerun; the earlier Windows stall remains unresolved and is not a passing gate. No detector, optical payload or reference-tree changes.
- Isolated backend 18095 / admin 13095 used disposable synthetic state: eight localized phones at uneven x positions split 2/2/2/2; one manual Center right member made the displayed counts 2/2/3/2. Changing presets to a shared lane or Silent and saving updated assignments and durable checkpoint without recalibration. Four cards, distinct dot colors and unequal-width map boundaries were visually inspected. Narrow map bands omit text labels; cards remain labeled.
- Draft recovery UI check: changed show name and Center right preset, reloaded the isolated page, verified both unsaved edits remained, saved and saw updated group music plus disabled Save. Unit tests reject stale revision/show/session recovery and tolerate unavailable/invalid browser storage. Recovery stores show metadata in this tab's sessionStorage, never the audio bytes.
- Isolated Next first failed because copied tsconfig inherited a relative parent; corrected only the ignored copy to use the root config. Its successful supervisor PID 48048 was stopped with its verified process tree after testing. The disposable test tab was closed. A former test tab navigated by the user to the public projector was preserved.

## Live activation and show-save incident

The user observed `Show request did not match the protocol v1 schema` during development. Root cause: Next hot-reloaded the new section-preset editor while the existing Bun backend still held the old strict Show schema. The uploaded percussion audio was intact and present in the unsaved editor. Before restarting, added session-scoped draft recovery and persisted that draft via the Save action; it remained visible. A transient React Fast Refresh dependency-array warning occurred during that in-place migration; the normal mounted editor and full-reload verification pass.

Copied the durable checkpoint to ignored `runtime/branch-restart-20260920/checkpoint-before-sections.json`, verified and stopped old launcher PID 40528 and its application tree, then started `bun scripts/dev.ts all` hidden as PID 41364. Logs: `app-sections.stdout.log` / `app-sections.stderr.log` in that same runtime folder. New backend epoch: `a0ea0d74-72fd-4840-abfd-ea57d2d36447`; two device identities restored. Tunnel/configuration and original assets preserved.

After activation the public UI and checkpoint both confirmed the user's show saved successfully at revision 4 with melody.mp3, vocals.mp3 and percussion.mp3. Retained the user's selected presets (Left/channel-3, Center left/channel-2, Center right/channel-2, Right/channel-0) rather than imposing defaults. Public GETs for all three stored asset URLs returned 200; downloaded sizes 8,196,528 / 8,098,418 / 11,021,760 bytes and all SHA-256 values matched the saved metadata. No live calibration or playback was triggered for testing.

Operator guides, architecture and integration handoff updated. Participant browsers must refresh for the additive strict snapshot fields. Physical camera placement, four-section phone playback and acoustic synchronization remain venue checks; synthetic/browser checks do not establish physical acceptance.

## Publication handoff

The user confirmed the live result works and explicitly requested commit/push. Fetched origin/main and verified it matches the local baseline 1e66b42 with no divergent commits. Reviewed the final diff and preserved the running service and ignored live state. Publish the upload fix as one semantic commit and automatic sections/draft recovery as a second, using the gate and browser evidence above; only context documentation changed after those checks. No forced push, deployment restart or additional feature change is needed.
