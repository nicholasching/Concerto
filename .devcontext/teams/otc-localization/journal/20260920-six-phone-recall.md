# Six-phone physical recall investigation

Baseline: integrated main with the OTC v2 working-tree changes preserved. User asks why the newest six-phone run detected four and missed two. Scope: inspect original saved evidence and reproduce tracking/sampling offline; no live state or implementation changes.

Matching latest job `a84dda2a-4982-4ea6-9e48-2e719502b18d`, run `e4cdd587-20f1-4616-82c8-b811cc8cd020`, internal tag 24, participants 0..5. Input SHA-256 `f328e80c1d926c96e67478c9ac5fa114dafd132b411c6c231b6ce749e87e8a66`. Result accepts 0/1/2/5 with agreeing passes and zero corrections/erasures; 3/4 unseen. Manifest is still **otc-v1, 200 ms**, while decoderVersion is otc-v2.0. Thus this is not physical validation of the v2 flash sequence. Saved diagnostics contain only four header-qualified tracks; missing phones failed before identity decoding.

Plan: reproduce original scan, retain ignored local diagnostics, locate the missing visible screens, inspect per-slot measurements and tracking continuity. Compare measured color transitions with the frozen v1 header; do not identify devices by forcing membership or change a v1 manifest to v2. Document demonstrated mechanism separately from runtime-version inference. Verification command: isolated Python probe in `runtime/otc-six-phone-investigation/` against the original upload.

## Results

- Original input hash verified. Fresh four-worker scan reproduces the saved four accepted observations exactly; same centers, track IDs, phase 2576.3222 ms and 2,368 candidates without resolved headers. Normal diagnostics hide these candidates, so there are zero published rejections despite two visible missing phones.
- **ID 3, second phone from the left:** the red detector processes hue 0..10 and 170..179 as separate components. At PTS 3033.8222 ms the same red phone yields a lower 24x26 component centered (642.16,531.18), an upper 32x51 component centered (646.40,504.33), and a small 6x12 fragment. The broad bright component also includes surroundings (88x85, fill .3836), below the .75 threshold that would reunite the pieces. Blue becomes one 34x58 component at 3633.8333 ms. Tracks fragment and report ambiguous association/merge. The first retained substantial phone fragment starts only at 3700.5 ms; no continuous pilot/preamble track reaches identity decoding.
- **ID 4, tilted phone near the middle:** at PTS 3633.8333 ms its blue interior is approximately RGB (57,75,163), below the detector's V>=190 blue-core threshold. There is no blue-core candidate. The generic dim mask connects the phone to nearby skin/background into a 219x122 component, so it does not retain an isolated screen. At the earlier red pilot, the same phone has RGB (243,37,54) and an isolated 42x46 red component. This color-dependent footprint loss breaks the pilot/preamble track. The reason its blue is dimmer (display brightness/viewing angle/exposure) is not independently measured.
- **Independent diagnostic, not an automatic production fix:** manually selected screen ROIs read measured red/blue pixels through the original clip, using the same production symbol sampler and unchanged full v1 packet decoder. Both complete headers and tag 24 validate. ID 3 and ID 4 each decode with agreeing passes, zero corrected bits and zero erased bits, score 1.0. Membership was not used to select colors or fill bits. This proves recoverable signals are present and the automatic segmentation/tracking step is the obstacle; it does not prove automatic six-phone recovery.
- **Runtime mismatch confirmed:** backend port 8080 belongs to Bun PID 38680, started 2026-09-19 22:41:22 local, predating v2 implementation. Audience/admin Node processes on ports 3000/3001 started at the same time. Source now creates v2/250 ms plans, but this job's frozen manifest is v1/200 ms. Python loads current worker code per job, explaining decoderVersion otc-v2.0 on a legacy sequence. No restart or live state mutation performed.

## Evidence and handoff

Commands: `$env:PYTHONPATH='workers/otc/src'; .venv/Scripts/python.exe runtime/otc-six-phone-investigation/probe.py`, then `detail.py` and `segmentation.py`. All complete successfully. Ignored local evidence: original scan cache, exact-frame extracts, `report.json`, `detail.json`, `frames.json`, `segmentation.json`. Initial diagnostic helper accidentally supplied an empty list instead of a zero-valued exclusion mask, producing empty frame detections; corrected and reran before interpreting component evidence. Production replay was unaffected.

No production implementation changes or automated test-suite rerun in this diagnostic task. Next targeted repairs: union the two red hue ranges before connected-component extraction; preserve compact dim blue screens without absorbing nearby skin/clothing. Add regressions representing these two mechanisms, replay this exact clip and previous red/amber physical clips, and run the OTC gate. Activate updated backend/frontend and create a new v2 run for 250 ms physical validation; slower symbols and more parity cannot alone repair an absent screen track. Keep any restart separate from this read-only investigation because it ends pending calibration state.
