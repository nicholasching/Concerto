# Recover the two screens in the physical sample

Date 2026-09-19; captain on main, baseline 6842d51. User explicitly requested committing/pushing every outstanding branch, then optical calibration against the attached sample with two flashing devices.

Publication: all worktrees inspected; only outstanding file was the admin paused-verification journal. Committed it as 189d0f4 on feat/admin-console. Atomic push published main 6842d51, sync a35ce11, client b71425a and admin 189d0f4; OTC 17299a1 was already current. No force push or worktree switching. Continue optical changes on main; teammates retain their branch ownership.

Input is the same original 1920x1080 H.264 sample, SHA-256 463b19f376ce050b9b0aa6732931b8e4b8db345df2a3bd10dc8ad928179096f1. Use the retained run-tag-4 manifest with participants [1,8,9,11] and one actual camera view. User states two devices should decode; do not inject that count or identities into the detector. Raw footage/frames stay ignored/local.

Plan: characterize screen segmentation through the full pilot/data sequence; isolate palette/brightness regions from background and preserve conservative tracking; add independent synthetic regressions for the measured failure; verify exactly two accepted unique packet IDs from the untouched original file plus wrong-tag/participant/negative checks; run gate:otc and record physical limits. Candidate-map review stays separate from authoritative commit; no fabricated geometry.

Experiments (local artifacts under ignored `runtime/camera-diagnosis/`):

- The rear amber screen measures approximately RGB [255,253,214], HSV saturation 41: the previous saturation floor 55 misses its core. Blue emission joins dim clothing into a 394x308 component; the other amber phone can join a 590x560 background component.
- A bright core mask (S>=25, V>=190) preserves the washed amber and separates emissive screens from dim surroundings. A 3x3 opening removes a thin blue bloom bridge: measured component changes from 45x58 to 27x55 without expanding nearby screens. Preserve a dim candidate path for lower-brightness phones.
- Bright-only cached tracking with area gating recovers exact agreeing packets for IDs 9 and 11; 9 still carries geometry ambiguity from bloom/adjacent fragments. This is diagnostic evidence, not a final accepted result.
- Combined bright/dim masks with opening and area gating accept 11 but lose 9. Adding predicted-footprint overlap alone still accepts only 11 (79.98 seconds, 1,306 frames). Cached trajectories show dim background fragments stealing the rear phone track at the first blue transition. These attempts are failures against the requested two-screen criterion; identity/tag/error bounds are unchanged.
- Intensity-continuity experiment follows: constrain frame-to-frame peak RGB changes as well as position, footprint and area, so a dark background fragment cannot become a bright phone merely because it is nearby. Cached candidate replay accelerates investigation; final evidence must reread the unchanged original video with the production CLI.

The continuity experiment recovered both 9 and 11 with zero corrected bits/erasures and agreeing bounded passes. A production CLI reread (`physical-v14.result.json`) confirmed exactly these two accepted tracks in 67.69 seconds, PTS phase 29,534.34 ms. Their centers were approximately (1012.93,579.21) and (1359.12,660.86), matching the two visibly flashing screens. Exact sampled packets reject the wrong run tag and reject either identity removed from membership. No geometry anchors means column-only locations with null x/y.

First full regression gate: 86 passed, two failed. One failure was a real small-screen regression: the bright mask clipped a compressed 10x16 amber screen to a 4x12 seed in its first frame, making the subsequent full screen look like an abrupt size change. Resolve this by using a small dim component containing at least 80% of exactly one bright core, capped at four times that core's bounding area. Large clothing/glare components cannot replace cores, and multi-core components remain separate/ambiguous. Added a direct seed-footprint test; degraded MP4 recall is restored without changing size-ambiguity bounds.

The other failure required at least one rejection in the old 90-phone, 640x360 crowding fixture. All 90 now localize correctly. Updated that test to require correct positions/columns and correct IDs at every accepted camera position, while permitting improved recall. Unresolvable 1x2 screens, actual merges/crossings, occlusion, duplicate IDs, wrong-tag and membership rejection remain independent gates. The new emissive-background MP4 fixture independently renders washed pilots over clothing/glow, different brightness between pilots, and dim phones; all 12 recover with correct identities and geometry.

Final full gate and live console processing are running after the footprint fix. The console now supports selecting one camera view and avoids treating three copies of this same file as independent camera columns. No packet/codebook/schema/dependency change; decoder version increments to otc-v1.4.

The next full gate passed 89 Python tests and shared checks, but independent original-video verification caught another regression: allowing *strong* cores to grow into modest dim components shortened the right phone's track and rejected its incomplete run tag. Live job `bb11daa0-af38-44d0-b0d8-d895db667333` accepted only 9 in 82.6 seconds and remains a failed experiment. Limit footprint recovery to faint cores with peak sampled RGB below 210, near the 190 pixel cutoff; strong emissive cores retain their own footprint. Added a modest-clothing-halo regression. All 16 focused tracking/degraded/emissive tests pass. Final production reread and full gate follow; previous successful or failed attempts are not silently replaced.

## Final verification

- `bun run gate:otc`: **90 Python tests pass**, 108.34 seconds, plus all shared schema/fixture/boundary/type/lint checks and 14 contract tests. Separate Ruff over workers/otc and tools/otc-fixtures passes. No errors/erasures, membership, duplicate-ID, crossing, phase or tag acceptance bounds were loosened.
- `bun run gate:admin`: **21 tests pass**, 14 contracts and shared checks pass; production build passes. Real browser verification confirms duplicate selected hashes block processing; zero selected cameras block processing; center-only selection submits a one-camera manifest; inputs are disabled while processing.
- Live job **966bee6c-40e4-4bdd-86c4-f57f94b1afb8**, final `otc-v1.4`, completed in **81.88 seconds** using the original file hash above, 1,306 frames at native 1920x1080 resolution. Exactly **two accepted IDs: 9 and 11**, both score 1.0, zero corrected/erased bits, both bounded passes agree. Their median centers remain (1012.93,579.21) and (1359.12,660.86). The inferred packet phase is 29,534.34 ms, derived solely from pilots/preamble.
- Wrong-tag and out-of-participant-set checks on the final sampled packets reject both phones. The other eligible IDs 1 and 8 remain unseen; no IDs/expected count/known screen coordinates were added to production code.
- Live console visibly reports `2 devices decoded; 0 with seat coordinates; 2 with column only. 4 eligible phones.` Its device evidence lists 9 and 11 as center/optical-column. Original uploads and active run are preserved. Candidate remains **uncommitted**, with review checkbox unchecked and authoritative map revision 2 unchanged.
- Local-only artifacts: `runtime/local/jobs/966bee6c-40e4-4bdd-86c4-f57f94b1afb8.{manifest,result}.json` and `.result.json.debug/`; compact negative-check summary at `runtime/camera-diagnosis/final-physical-summary.json`. Raw footage and debug pixels remain outside Git.
- Existing three-camera 4K/1,500-phone clean synthetic benchmark rerun with final code: **1,500/1,500 localized**, no wrong localized IDs, maximum canonical error 0.000531 (tolerance 0.015), **54.42 seconds** worker time / 54.96 wall, 849,313,792 bytes sampled aggregate resident peak (~810 MiB), three camera processes. Report: `runtime/otc-fixtures/density-1500/parallel-v14/metrics.json`. This is slower than v1.2's 30.83-second single-mask run because the real-scene fix evaluates two masks, but remains below the 90-second synthetic target on this machine. It is not physical crowd/venue evidence.

Reproduction: prepare the repo Python environment; run `python -m otc process --manifest <saved manifest> --output <new result path> --evidence physical --debug-dir <new debug directory>`. Manifest must contain the original clip with its verified hash, run tag 4 and captured membership [1,8,9,11]; camera geometry remains null. On this machine the manifest above is preserved. Other machines need the original file supplied outside Git and must update its local videoPath; do not reuse its run tag for a different recording.

Limits: the user's independently stated visible count is two; IDs are recovered from emitted packets and matched to those screen positions, not independently human-labeled physical devices. One camera and no seating anchors establish optical identities/column evidence only, not seat geometry. This sample does not establish near/far venue recall, three-camera calibration, large exposure/handheld-motion robustness or concert acceptance. Background fragments are rejected and reported (1,375 retained rejected observations, 71,855 retired short fragments); explicit stage/light exclusions can still help cluttered captures.
