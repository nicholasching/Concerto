# Wider color detection and v2 activation

User authorizes widening detection for different/dim phone screens, replaying the last six-phone video, and activating v2 in the running app. Preserve existing OTC v2 work and live persistent show/map/device state. Own OTC detector/tests and integration runtime activation; no shared wire change required.

Plan: combine red hue wraparound before component extraction; add wider/fainter color candidates after strong cores so background halos cannot replace an already isolated screen. Keep measured pilot-relative bit decisions, full-codebook correction, collision/identity checks. Reproduce both physical failures with synthetic regression scenes. Run the root OTC gate, replay last six-phone/red-four/amber-five originals, then restart the coordinated app processes and verify a live prepared v2/250 ms plan. No automatic flashes or map overwrite. Success: automatic six-phone recovery without manually chosen regions or identity fitting; legacy regressions retain accepted IDs; new live runs advertise v2/250 ms.

Parity detects/corrects bounded errors after a usable track exists. It is not proof that arbitrary reflections or mixed screen tracks are safe; broader candidate discovery is followed by existing temporal/identity checks.

## Implementation and experiments

- Red's two HSV ranges are unioned before contour extraction. Strong existing cores remain first; wider faint-color passes admit V>=50 and S>=80 versus the original V>=190/S>=140 core thresholds. Blue hue expands from 85..135 to 80..145; red fallback covers 150..179 and 0..20 in staged passes, with a low-brightness pass to separate dim orange-shifted screens from brighter skin.
- Faint candidates must retain a compact rotated footprint (>=75% occupancy of the oriented rectangle). A broad color halo cannot overwrite an isolated core. Bright-footprint recovery checks color compatibility so a solid warm skin patch cannot replace a red screen. These geometry checks operate before identity and do not fit participant IDs.
- Association now tolerates fourfold brightness differences between colors (previously 1.67-fold), uses recent median screen dimensions and skips a sudden >3x footprint expansion while retaining the track for subsequent frames. Existing camera motion/overlap/duplicate and full-codebook checks remain. Ambiguous color votes remain erasures; no hard-coded phone locations.
- Failed experiments preserved: first naive wider masks recovered ID 3 but lost ID 2 and still missed 4. Instrumentation proved a 247-to-147 brightness transition failed the old .6 ratio, and wide magenta glow distorted another track. Median footprints alone and expansion skipping alone were insufficient; compact faint-color footprints recovered all six. A new compressed six-phone/three-camera faint-color fixture then exposed warm skin replacing a red core and dim orange blending with a brighter same-hue surround; staged brightness and color-compatible restoration fixed these as well. Intermediate `tolerant*.result.json` and scan probes remain ignored.

## Verification in progress

- 33 tracking tests pass, including new red wraparound, faint/shifted colors, strong-core halo protection, fourfold brightness changes and transient footprint expansion. New compressed-video faint-color test passes with exact independently generated IDs/positions; existing empty/duplicate/crossing v2 checks also passed during development. Full root OTC gate running on final implementation.
- Final original six-phone replay (`final-six.result.json`): **6/6 accepted and localized**, IDs 0..5, agreeing passes, zero corrected/erased bits for every phone. Original manifest/hash unchanged; fully automatic scan with no ROIs or identity fitting. Previous saved result was 4/6. This is a physical v1 recording and does not substitute for a fresh v2 capture.
- Final old red-four replay (`final-red4.result.json`): IDs 0/1/2 retained; fourth phone still correctly fails its old optical run tag. Earlier amber-five replay passed 5/5; final repeat after the last detector refinements is running. Concurrent replay timings include competition with the gate and are not comparable performance measurements.
- Pre-restart snapshot/checkpoint backed up in ignored investigation folder. No participants connected, transport stopped, no active calibration; seven registered IDs 0..6, map revision 3 and show revision 3. Runtime activation remains pending gate completion. Live prepared-plan verification requires a connected phone, so do not register a synthetic device in the real session merely to produce that evidence.

## Final scope and runtime activation

- The extra compatibility replay caught the new bright-footprint color check regressing amber 5/5 to 4/5. Restricted that check to actual red cores in red-palette runs. Final source replays `final2-six.result.json` and `final2-amber5.result.json` pass 6/6 and 5/5 respectively. Six-phone identity bits are all error/erasure-free; amber has zero corrections and up to two erasures. Final focused tracking/faint/negative-scene tests: 37 passed. Earlier root gate passed 194 tests in 268.02 s; a second root gate validates the completed palette-scoping edit.
- At 03:50:58 local, restarted only the verified app supervisor/backend/audience/admin process tree, leaving the tunnel running. New backend PID 11164; audience/admin listeners 5244/39816. New server epoch `792247b2-3d50-47e8-b216-5b3f6a794b16`. Snapshot comparison confirms show, audience map, assignments and registered IDs unchanged; no active run or playback was interrupted. `activate.ts after` passes.
- Served `/` and `/admin` HTTP 200 and compiled JavaScript contain v2 packet encoding and duration helpers. Public `https://htn.nicholasching.ca/api/session` returns HTTP 200 and the same new backend epoch. No connected physical phones were available for a live prepared-plan check; no fake participant was registered. Fresh pages and the next new calibration use updated v2 code. A fresh physical v2 flash remains to be recorded.
- Isolated real HTTP/WS/Python E2E passed: four actual test joins, generated red/blue videos, decoding/map review/commit, routing, late join, scheduling/mix/panic and restart preservation. Runtime `runtime/e2e/482bda46-5634-41a1-926f-491686b5b023/`; capture manifest is checked separately for v2/250 ms. This fixture uses a separate disposable session and does not change the live map.
- The completed-source gate reached 193 passing tests, with one infrastructure failure: Windows `WinError 1455` creating a shared-memory mapping in the emissive-background video test (paging-file capacity), during frontend recompilation/E2E activity. No assertion mismatch. Full gate retry uses supported `OTC_CPU_BUDGET=3` without changing production configuration or skipping tests. Log `verified-budget3-gate.log`. The earlier 194-pass checkpoint is retained separately and does not substitute for this final-source retry.

## Completed verification and next action

Final root `gate:otc` with `OTC_CPU_BUDGET=3` **passes** on the completed implementation: generated schemas/fixtures, import boundaries, TypeScript types/lint, 15 contract tests, worker Ruff and **194 Python tests in 221.96 s**. Fixture-tool Ruff and `git diff --check` pass. No tests skipped to resolve the memory failure. `beatsync-source/` remains unchanged. Full v2 HTTP/WS/video E2E and served local/public app checks pass separately.

User-requested software work and runtime activation are complete. Refresh admin and participant pages, create a new calibration, and record the 47-symbol / 250-ms / 11.75-second v2 sequence. Current committed live map remains the user's existing revision 3; the offline six-phone replay is evidence, not an automatic map replacement. The captured six-phone test proves automatic 6/6 recovery on this specific original v1 clip, not venue-scale recall or a fresh physical v2 run. No commit/push performed.
