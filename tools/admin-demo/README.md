# Operator harness - Team 4

bun run dev:admin-demo starts this loopback mock on 18084 and Next on 3001. Current scope: 1,500 synthetic dots plus snapshots/assets/probes/broadcasts. Add command ACK/job/revision/reconnect scenarios here behind frozen contracts. Dot count is not load evidence. Production UI ignores the mock flag.
